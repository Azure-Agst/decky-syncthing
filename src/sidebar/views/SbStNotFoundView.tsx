import { VFC } from "react";

export const SbNotFoundView: VFC = ({}) => {

    return (
        <div style={{ margin: "auto" }}>
            <div>It looks like you don't have SyncThing installed properly!</div>
        </div>
    )
}